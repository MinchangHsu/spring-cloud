server:
  port: 8000

spring:
  application:
    name: spring-cloud-eureka-user-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    metadata-map:
      #可自訂的中繼資料 key-value 格式
      customer: Minchang Hsu
